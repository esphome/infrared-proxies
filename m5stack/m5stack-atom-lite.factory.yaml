packages:
  m5stack-atom-lite: !include m5stack-atom-lite.yaml

esphome:
  project:
    name: esphome.infrared-proxy
    version: dev

improv_serial:

ota:
  - platform: http_request
    id: ota_http_request

update:
  - platform: http_request
    id: update_http_request
    name: Firmware
    source: https://firmware.esphome.io/infrared-proxy/m5stack-atom-lite/manifest.json

http_request:

dashboard_import:
  package_import_url: github://esphome/infrared-proxies/m5stack/m5stack-atom-lite.yaml@main
